spring.application.name=backend


# Configurações do PostgreSQL
# spring.datasource.url=jdbc:postgresql://localhost:5432/Projeto3
# spring.datasource.username=luimanso
# spring.datasource.password=luii2244
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Configuração para logar as consultas SQL (opcional)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configurações de pool de conexão (opcional)
spring.datasource.hikari.maximum-pool-size=10